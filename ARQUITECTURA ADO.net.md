![[Pasted image 20260226164856.png]]

hay que crear la clase de cada tabla del sql
tambien la clase de la conexion

el controlador expone los endpoints a internet

que son los modelos en esta arquitectura.
los modelos en esta arquitectura son la representación de las tablas dentro de mi proyecto, tablas que previamente ya cree en mi modelo de base de datos.
